/* arr1 is valid since expression 0 is int = myint; test02 with chk & output */
let
    type myint = int
    type  arrtype = array of myint

    var arr1: arrtype := arrtype [10] of 0
in
    if (arr1[4] = 0)
    then print("SUCCESS")
    else print("FAIL")
end

