PROC {name = tigermain, formals = t, locals = 1}
SEQ(
  EXP(
    CONST 0),
  SEQ(
    EXP(
      CONST 0),
    SEQ(
      MOVE(
        RV,
        ESEQ(
          MOVE(
            MEM(
              BINOP(PLUS,
                FP,
                CONST ~4)),
            CALL(
              NAME initArray,
              CONST 10,
              CONST 1,
              NAME L1_string,
              CONST 4,
              CONST 26)),
          MEM(
            BINOP(PLUS,
              FP,
              CONST ~4)))),
      EXP(
        CONST 0))))

STRING L1_string = "../testcases/test52.tig"

STRING L0_string = "DefaultString"

